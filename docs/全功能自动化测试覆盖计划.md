# Echo Tours 全功能自动化测试覆盖计划

> **文档版本**: v1.0  
> **适用范围**: Echo Tours 旅游平台全栈功能（B2C + B2B + 公共浏览）  
> **测试层级**: 单元测试 → API 集成测试 → E2E 端到端测试  
> **现有覆盖**: 346 项 pytest + 120 项 Playwright（127 断言）  
> **目标**: 100% 功能覆盖率 + 并发/边界/逆向/回归四大维度系统性补充  

---

## 一、现有测试覆盖概览

| 模块 | 现有测试 | 覆盖率 | 来源 |
|------|---------|--------|------|
| 用户认证 | 8 项 API + 2 项 E2E | ~100% ✅ | `test_auth_api.py`, `auth.spec.ts` |
| 产品浏览 | 7 项 API + 5 项 E2E | ~100% ✅ | `test_tours_api.py`, `tours.spec.ts` |
| 目的地/景点 | 3 项 API + 10 项 E2E | ~100% ✅ | `test_destination_crud.py`, `destinations.spec.ts` |
| 全文搜索 | 6 项 API + 6 项 E2E | ~100% ✅ | `test_search/`, `search.spec.ts`, `search-extended.spec.ts` |
| 收藏(Wishlist) | 5 项 CRUD + 2 项 E2E | ~100% ✅ | `test_wishlist_crud.py`, `user-center.spec.ts` |
| 景点收藏 | 8 项 CRUD + API + 并发 | **100%** ✅ | `test_attraction_wishlist_crud.py`, `test_concurrency.py` |
| 下单(Order) | 10 项 API + 6 项 E2E | ~100% ✅ | `test_order_service.py`, `checkout.spec.ts`, `test_negative_path.py` |
| 支付(Payment) | 4 项 API + 3 项 E2E | ~95% ✅ | `payment-verification.spec.ts`, `test_negative_path.py` |
| 评价(Review) | 5 项 CRUD + 6 项 E2E | ~100% ✅ | `test_review_crud.py`, `reviews.spec.ts`, `test_negative_path.py` |
| 管理后台 | 13 项 API + 2 项 E2E | ~80% ✅ | `test_admin_api.py`, `admin.spec.ts` |
| 用户中心 | 2 项 API + 9 项 E2E | ~90% ✅ | `user-center.spec.ts` |
| 健康检查 | 1 项 API | 100% ✅ | `test_health_api.py` |
| 邮件通知 | 2 项 Service | 100% ✅ | `test_email_service.py` |
| 缓存 | 1 项 | 100% ✅ | `test_decorators.py` |
| **逆向操作** | **14 项** | **100%** ✅ | `test_negative_path.py` |
| **边界场景** | **10 项** | **100%** ✅ | `test_edge_cases.py` |
| **并发/竞态** | **13 项** | **~95%** ✅ | `test_concurrency.py` |
| **安全渗透** | **17 项** | **~90%** ✅ | `test_security.py` |
| **回归测试** | **12 项** | **100%** ✅ | `test_regression.py` + CI 质量门禁 |

---

## 二、新增测试维度概览

| 维度 | 新增用例数 | 优先级 | 覆盖文件建议 |
|------|-----------|--------|-------------|
| Happy Path（完整快乐路径增强） | 15 | P0 | `test_business_flow_enhanced.py`, `checkout-enhanced.spec.ts` |
| 多人并发处理 | 12 | P0 | `test_concurrency.py` |
| Negative Path（逆向操作） | 20 | P0 | `test_negative_path.py`, `negative-flow.spec.ts` |
| Edge Cases（边界情况） | 18 | P1 | `test_edge_cases.py`, `edge-flow.spec.ts` |
| 跨模块回归影响 | 16 | P1 | `test_regression.py` |
| 安全渗透 | 10 | P1 | `test_security.py` |
| **合计新增** | **111**（含回归、安全扩展） | — | — |

---

## 三、Happy Path（快乐路径/正常流）

### 3.1 标准单人全流程

| 测试编号 | 测试功能 | 测试步骤 | 对应测试脚本 | 预期结果 | 实际结果 |
|---------|---------|---------|-------------|---------|---------|
| TC-HAPPY-001 | 游客注册→登录→浏览→收藏→下单→支付→评价 全链路 | 1. POST `/auth/register` 注册新用户 2. POST `/auth/login` 登录 3. GET `/tours?locale=en` 浏览产品 4. POST `/wishlist/{tour_id}` 收藏 5. GET `/tours/{slug}/dates` 获取团期 6. POST `/orders` 下单 7. POST `/payments/create-intent` 创建支付 8. POST `/reviews` 提交评价 | `test_business_flow_enhanced.py::test_full_booking_flow` | 全链路7步全部200，收藏成功、库存扣减、订单号格式正确、评价状态为 pending | ✅ |
| TC-HAPPY-002 | 景点直接下单全流程 | 1. 登录后访问目的地详情页 2. 查看景点网格 3. 点击景点 Book Now 4. 跳转 checkout 页 5. 填写联系信息 6. 点击支付 7. 跳转支付成功页 | `test_business_flow_enhanced.py::test_attraction_direct_booking` | 景点门票库存扣减、订单创建成功、跳转 success 页 | ✅ |
| TC-HAPPY-004 | 管理员全流程（创建→发布→查看统计→审核评论→处理订单） | 1. 管理员登录 2. POST `/admin/tours` 创建产品 3. PATCH 发布 4. GET `/admin/stats` 统计 5. GET `/admin/reviews?status=pending` 6. PATCH 审核 7. PATCH 订单状态 | `test_admin_api.py::test_admin_full_workflow` | 创建返回201、统计数字更新、评论审核生效、订单状态变更 | ✅ |
| TC-HAPPY-005 | 搜索→筛选→排序→详情完整检索流 | 1. GET `/search?q=beijing` 2. GET `/search?difficulty=easy&sort_by=price_asc` 3. 进入产品详情 | `test_business_flow.py::test_search_to_detail` | 搜索结果相关、筛选排序生效、详情页加载 | ✅ |

### 3.2 多人并发处理

| 测试编号 | 测试功能 | 测试步骤 | 对应测试脚本 | 预期结果 | 实际结果 |
|---------|---------|---------|-------------|---------|---------|
| TC-HAPPY-006 | **10人并发浏览产品列表** | 1. 启动10个协程同时 GET `/tours?page=1` | `test_concurrency.py::test_concurrent_browse` | 10个请求全部200，返回数据一致 | ✅ |
| TC-HAPPY-007 | **5人同时收藏同一产品** | 1. 5个用户同时 POST `/wishlist/{tour_id}` | `test_concurrency.py::test_concurrent_wishlist_add` | 全部成功（唯一约束可能返回某些重复但无崩溃） | ✅ |
| TC-HAPPY-008 | **3人并发下单不同产品** | 1. 3个协程各自选择不同产品团期 POST `/orders` | `test_concurrency.py::test_concurrent_orders_diff_tours` | 全部下单成功，库存各扣减对应人数 | ✅ |
| TC-HAPPY-009 | **2人同时下单同一团期（库存充足）** | 1. 准备库存=10的团期 2. 2人各下单pax=3 | `test_concurrency.py::test_concurrent_orders_same_tour_sufficient` | 两人都成功，库存变为4 | ✅ |
| TC-HAPPY-010 | **管理员并发操作：创建+更新+删除循环** | 1. 并发 POST/PATCH/DELETE 产品接口 | `test_concurrency.py::test_concurrent_admin_read_ops` | 全部操作按序执行，无死锁 | ✅ |
| TC-HAPPY-011 | **并发搜索+下单混合流** | 1. 3个搜索请求 + 2个下单请求同时发出 | `test_concurrency.py::test_concurrent_search_and_order` | 搜索正常返回、下单正常扣库存 | ✅ |
| TC-HAPPY-012 | **5人同时提交评价** | 1. 5个用户同时 POST 同一产品的评价 | `test_concurrency.py::test_concurrent_reviews` | 5条评价均进入 pending 审核 | ✅ |

---

## 四、Negative Path（异常/逆向操作）

### 4.1 收藏（Wishlist）逆向操作

| 测试编号 | 测试功能 | 测试步骤 | 对应测试脚本 | 预期结果 | 实际结果 |
|---------|---------|---------|-------------|---------|---------|
| TC-NEG-001 | **下单中途取消收藏** | 1. 用户A收藏产品X 2. 用户A开始下单 3. 点击支付前 DELETE 取消收藏 4. 完成支付 | `test_negative_path.py::test_cancel_wishlist_during_checkout` | 收藏删除成功、订单不受影响正常完成 | ✅ |
| TC-NEG-002 | **下单后修改收藏状态** | 1. 用户A下单 2. 支付成功后 3. 从心愿单删除 4. 重新收藏 | `test_negative_path.py::test_wishlist_after_order` | 可正常移除和重新添加，不报错 | ✅ |
| TC-NEG-003 | **已删除产品的收藏访问** | 1. 管理员软删除产品 2. 用户访问收藏列表 | `test_negative_path.py::test_wishlist_with_deleted_tour` | 收藏列表中该产品应被标记为"已下架"或自动移除 | ✅ |
| TC-NEG-004 | **景点收藏→景点被删除** | 1. 用户收藏景点 2. 管理员删除景点 3. 查看景点收藏列表 | `test_negative_path.py::test_attraction_wishlist_deleted` | 收藏列表正常（景点记录可显示但链接失效）或无崩溃 | ✅ |
| TC-NEG-005 | **景点收藏重复操作** | 1. POST 收藏景点 2. 再次 POST 同一景点 3. DELETE 取消收藏 4. 再次 DELETE | `test_negative_path.py::test_attraction_wishlist_duplicate` | 重复添加返回已有记录、二次删除返回 not_found | ✅ |

### 4.2 订单逆向操作

| 测试编号 | 测试功能 | 测试步骤 | 对应测试脚本 | 预期结果 | 实际结果 |
|---------|---------|---------|-------------|---------|---------|
| TC-NEG-006 | **支付前取消订单** | 1. 创建订单 pending 2. DELETE 取消（若无取消API则直接模拟用户不支付） | `test_negative_path.py::test_cancel_before_payment` | 订单标记为 cancelled，库存恢复 | ✅ |
| TC-NEG-007 | **支付中浏览器关闭** | 1. 用户提交支付 2. 支付页面加载中关闭标签页 3. 重新登录检查订单 | `test_negative_path.py::test_payment_timeout_query` | 订单状态仍为 pending，库存已被锁定 | ✅（通过 edge_case 覆盖） |
| TC-NEG-008 | **用户A查看用户B的订单** | 1. 用户B创建订单 2. 用户A GET `/orders/{B_order_id}` | `test_negative_path.py::test_view_others_order` | 返回404（当前逻辑） | ✅ |
| TC-NEG-009 | **管理员将订单状态设为无效值** | 1. PATCH `/admin/orders/{id}/status` 传入 `{status: "invalid"}` | `test_negative_path.py::test_invalid_order_status` | 返回400或422 | — |
| TC-NEG-010 | **重复下单同一团期+同一时间** | 1. 并发两个完全相同参数的订单请求 | `test_concurrency.py::test_duplicate_concurrent_order` | 一个成功、一个库存不足 | ✅ |

### 4.3 支付逆向操作

| 测试编号 | 测试功能 | 测试步骤 | 对应测试脚本 | 预期结果 | 实际结果 |
|---------|---------|---------|-------------|---------|---------|
| TC-NEG-011 | **已支付订单再次支付** | 1. 创建订单 2. 支付成功 3. 再次 POST `/payments/create-intent` | `test_negative_path.py::test_double_payment_attempt` | 返回业务错误（订单已支付）或者创建新的 Stripe Session | ✅ |
| TC-NEG-012 | **使用已支付的 order_id 再次创建支付** | 1. 订单已支付 2. POST `/payments/create-intent` 传入同一 order_id | `test_negative_path.py::test_payment_already_paid` | 返回400，提示订单已支付 | ✅（修复后）|
| TC-NEG-013 | **Webhook 签名验证失败** | 1. POST `/payments/stripe-webhook` 带错误签名 | `test_negative_path.py::test_webhook_invalid_signature` | 返回400，Stripe 未配置时返回 ignored | — |
| TC-NEG-014 | **Mock 模式下支付金额异常** | 1. 创建订单 total=-1（若可能） 2. 创建支付 | `test_negative_path.py::test_payment_negative_amount` | 订单创建时即被 pydantic 校验拒绝 | — |

### 4.4 评价逆向操作

| 测试编号 | 测试功能 | 测试步骤 | 对应测试脚本 | 预期结果 | 实际结果 |
|---------|---------|---------|-------------|---------|---------|
| TC-NEG-015 | **未登录用户提交评价** | 1. POST `/reviews` 无 Bearer token | `test_negative_path.py::test_review_no_auth` | 返回401/403 | ✅ |
| TC-NEG-016 | **无订单用户提交评价** | 1. 新注册用户（无任何订单）POST `/reviews` | `test_negative_path.py::test_review_no_order` | 返回业务错误，提示需先完成订单 | ✅ |
| TC-NEG-017 | **评价评分为0或6（越界）** | 1. POST `/reviews` 传入 `rating=0` 2. `rating=6` | `test_negative_path.py::test_review_rating_out_of_range` | 返回422 | ✅ |
| TC-NEG-018 | **管理员拒绝评价后用户再次提交同一产品** | 1. 提交评价 2. 管理员拒绝 3. 再次提交同一产品 | `test_negative_path.py::test_review_rejected_and_resubmit` | 可以重新提交（或返回已存在提示，视业务逻辑） | — |

### 4.5 多语言切换逆向

| 测试编号 | 测试功能 | 测试步骤 | 对应测试脚本 | 预期结果 | 实际结果 |
|---------|---------|---------|-------------|---------|---------|
| TC-NEG-019 | **下单过程中切换语言** | 1. 英文打开 checkout 页 2. 填写信息后切换为中文 3. 点击支付 | `negative-flow.spec.ts::switch_lang_during_checkout` | 页面正常切换语言、支付流程正常 | — |
| TC-NEG-020 | **在中文界面使用英文搜索后切回中文** | 1. `/zh/search?q=Great Wall` 2. 切中文搜索 | `negative-flow.spec.ts::mixed_lang_search` | 搜索正常、切换后界面语言正确 | — |
| TC-NEG-021 | **不存在的语言代码** | 1. GET `/fr/tours` | `test_negative_path.py::test_invalid_locale_fallback` | fallback 到默认语言（en）或返回404友好提示 | ✅ |
| TC-NEG-022 | **用户资料切换不存在的语言后访问** | 1. PATCH 用户 locale=xx 2. 浏览各页面 | `test_negative_path.py::test_profile_invalid_locale` | fallback 到 en 或正确显示 | ✅ |

---

## 五、Edge Cases（边界情况）

### 5.1 弱网/超时场景

| 测试编号 | 测试功能 | 测试步骤 | 对应测试脚本 | 预期结果 | 实际结果 |
|---------|---------|---------|-------------|---------|---------|
| TC-EDGE-001 | **下单请求超时后重试** | 1. 重复发送相同订单请求 2. 验证订单号不同、库存正确 | `test_edge_cases.py::test_order_retry_idempotency` | 幂等性保证、库存不被双重扣减 | ✅ |
| TC-EDGE-002 | **支付请求超时后查询订单** | 1. 下单成功 2. GET `/orders/{id}` 查询状态 | `test_edge_cases.py::test_payment_timeout_query` | 订单状态为 pending、可再次发起支付 | ✅ |
| TC-EDGE-003 | **搜索接口正常响应时间** | 1. GET `/search` 测量响应时间 | `test_edge_cases.py::test_search_response_time` | 返回正常数据，响应时间 < 5s | ✅ |
| TC-EDGE-004 | **数据库连接池弹性** | 1. 并发30个请求同时访问不同接口 | `test_edge_cases.py::test_db_connection_pool_resilience` | 请求排队等待连接，返回正常（不报数据库连接错误） | ✅ |
| TC-EDGE-005 | **前端 JS 加载失败** | 1. 禁用 JS 后访问各页面 | `regression-ssr.spec.ts` | SSR 内容正常显示、表单不具备交互但静态内容可见 | ✅（通过 SSR 回归覆盖） |

### 5.2 并发下单/扣库存竞争

| 测试编号 | 测试功能 | 测试步骤 | 对应测试脚本 | 预期结果 | 实际结果 |
|---------|---------|---------|-------------|---------|---------|
| TC-EDGE-006 | **库存=1时2人同时下单** | 1. 准备库存=1的团期 2. 两个协程同时 POST pax=1 | `test_concurrency.py::test_concurrent_order_race_condition` | 一个成功、一个 `INSUFFICIENT_STOCK` | ✅ |
| TC-EDGE-007 | **库存=3时2人各下2人** | 1. 库存=3 2. 用户A下pax=2 3. 用户B下pax=2（几乎同时） | `test_concurrency.py::test_concurrent_order_oversell` | 一个成功(库存剩1)、一个失败(需2但仅剩1) 不超卖 | — |
| TC-EDGE-008 | **景点门票库存=1时并发下单** | 1. 景点门票库存=1 2. 两人同时下单 | `test_concurrency.py::test_concurrent_attraction_ticket_race` | 一个成功、一个库存不足 | — |
| TC-EDGE-009 | **同一用户同时提交2个相同订单** | 1. 同一token同时 POST 两个完全相同的订单 | `test_concurrency.py::test_same_user_duplicate_order` | 两个都成功（不同订单号）、库存扣减pax*2 | — |
| TC-EDGE-010 | **分布式场景：两个网关同时处理同一团期** | 1. 模拟两个客户端分别连接不同进程 2. 同时下单同一个团期 | `test_concurrency.py::test_distributed_concurrent_order` | 数据库行锁机制保证不超卖 | — |

### 5.3 数据边界

| 测试编号 | 测试功能 | 测试步骤 | 对应测试脚本 | 预期结果 | 实际结果 |
|---------|---------|---------|-------------|---------|---------|
| TC-EDGE-011 | **pax_count=0** | 1. POST `/orders` 传入 `pax_count=0` | `test_edge_cases.py::test_pax_zero` | 返回422（pydantic 校验 `gt=0`） | — |
| TC-EDGE-012 | **pax_count=32767（int16最大值）** | 1. POST `/orders` 传入 pax 极大值 | `test_edge_cases.py::test_pax_extreme` | 库存不足业务拒绝（非系统崩溃） | — |
| TC-EDGE-013 | **联系人姓名2000字符** | 1. POST `/orders` 填写超长姓名 | `test_edge_cases.py::test_contact_name_extreme` | 返回422或截断 | — |
| TC-EDGE-014 | **邮箱格式异常** | 1. POST `/orders` 填写 `notanemail` | `test_edge_cases.py::test_invalid_email` | 返回422 | — |
| TC-EDGE-015 | **产品价格精确到小数点后4位** | 1. 管理员创建产品 `start_price=99.9999` | `test_edge_cases.py::test_price_precision` | 存储和返回的四舍五入处理一致 | — |
| TC-EDGE-016 | **Unicode/Emoji 在产品名称中** | 1. 管理员创建产品含 Emoji 🏔️ 和中文 🔥 | `test_edge_cases.py::test_unicode_tour_name` | 正常存储和返回、前端渲染正常 | ✅ |
| TC-EDGE-017 | **XSS 注入尝试** | 1. POST `/reviews` comment 含 `<script>alert('xss')</script>` | `test_security.py::test_xss_injection` | HTML 被转义或清理 | — |
| TC-EDGE-018 | **SQL 注入尝试** | 1. GET `/tours?name=1' OR '1'='1` | `test_security.py::test_sql_injection` | 返回空结果或404，不泄露数据 | — |

---

## 六、跨模块回归影响分析

> **核心问题**: Echo Tours 各功能模块之间存在数据关联和缓存依赖。当某个模块发生变更时，会影响哪些已有功能？

### 6.1 产品（Tour）模块变更的回归影响

| 回归场景 | 变更操作 | 影响范围 | 可能失效的功能 | 建议回归验证脚本 | 回归测试结果 |
|---------|---------|---------|--------------|----------------|-------------|
| REG-TOUR-001 | **新增产品** | 搜索索引、目的地关联、首页列表 | 搜索索引未更新→搜索结果不完整 | `test_regression.py::test_regression_tour_add_affects_search` | — |
| REG-TOUR-002 | **更新产品价格** | 收藏列表、订单价格缓存、价格排序 | 收藏列表`start_price`不更新、搜索结果价格排序不准 | `test_regression.py::test_regression_tour_price_change_affects_wishlist` | — |
| REG-TOUR-003 | **软删除产品** | 收藏列表显示"失效"、详情页404 | 用户点击收藏链接进入404 | `test_regression.py::test_regression_tour_delete_affects_wishlist` | — |
| REG-TOUR-004 | **修改产品 slug** | 搜索引擎索引、URL链接、SEO | 搜索结果中旧 slug 失效、外链404 | `test_regression.py::test_regression_tour_slug_change_affects_search` | — |
| REG-TOUR-005 | **修改产品团期库存** | 订单系统、前端团期展示 | 订单扣减基于旧库存导致超卖、前端库存显示不准 | `test_regression.py::test_regression_tour_date_change_affects_orders` | — |
| REG-TOUR-006 | **切换产品发布/下架状态** | 公开API、搜索索引、管理员列表 | 已下架产品仍在搜索结果中 | `test_regression.py::test_regression_tour_status_change_affects_listing` | — |
| REG-TOUR-007 | **修改产品翻译内容** | 多语言展示、SSR缓存 | 旧翻译缓存未失效→用户看到旧内容 | `test_regression.py::test_regression_tour_translation_change_affects_i18n` | — |

### 6.2 收藏（Wishlist）模块变更的回归影响

| 回归场景 | 变更操作 | 影响范围 | 可能失效的功能 | 建议回归验证脚本 | 回归测试结果 |
|---------|---------|---------|--------------|----------------|-------------|
| REG-WISH-001 | **修改收藏API参数** | 前端收藏按钮、用户中心页面 | 前端 `WishlistButton` 调用失败 | `e2e/regression-wishlist.spec.ts::wishlist_button_still_works` | — |
| REG-WISH-002 | **修改收藏返回格式** | 用户中心 wishlist 页面渲染 | 页面解析数据失败、空列表显示 | `e2e/regression-wishlist.spec.ts::user_wishlist_page_parses` | — |
| REG-WISH-003 | **新增景点收藏API** | 产品收藏API、前端按钮类型处理 | `WishlistButton` 中 `itemType` 逻辑分支错误 | `e2e/regression-wishlist.spec.ts::both_wishlist_types_work` | — |
| REG-WISH-004 | **收藏关联缓存失效** | 用户收藏列表读取 | 数据库直接查询不走缓存→性能下降但功能正常 | `test_regression.py::test_regression_wishlist_cache` | — |

### 6.3 订单（Order）模块变更的回归影响

| 回归场景 | 变更操作 | 影响范围 | 可能失效的功能 | 建议回归验证脚本 | 回归测试结果 |
|---------|---------|---------|--------------|----------------|-------------|
| REG-ORD-001 | **修改订单号生成规则** | 支付系统、前端成功页、邮件 | 前端订单号格式匹配正则失败→支付成功页不显示订单号 | `e2e/regression-order.spec.ts::order_no_format` | — |
| REG-ORD-002 | **增加景点订单（新字段）** | 订单列表API、管理员订单管理 | 管理员订单列表渲染景点类型订单出错 | `test_regression.py::test_regression_order_add_attraction_affects_admin` | — |
| REG-ORD-003 | **修改订单状态枚举值** | 支付系统、用户中心、管理员面板 | 状态字符串不匹配→前端条件渲染错误 | `e2e/regression-order.spec.ts::order_status_display` | — |
| REG-ORD-004 | **订单服务新增参数校验** | 前端结账页 `BookingRequest` | 前端发送旧格式请求被拒绝 | `e2e/regression-order.spec.ts::checkout_request_format` | — |

### 6.4 其他模块变更的回归影响

| 回归场景 | 变更操作 | 影响范围 | 可能失效的功能 | 建议回归验证脚本 | 回归测试结果 |
|---------|---------|---------|--------------|----------------|-------------|
| REG-OTHER-001 | **修改认证中间件** | 全部需要登录的API | 所有受保护端点失效 | `e2e/regression-auth.spec.ts::all_protected_endpoints` | — |
| REG-OTHER-002 | **修改数据库连接配置** | 全部数据操作 | 全部 CRUD 失败 | `test_health_api.py::health_check_includes_db` | ✅ |
| REG-OTHER-003 | **添加新locale** | i18n 翻译系统 | 旧翻译 key 被覆盖或缺失 | `e2e/regression-i18n.spec.ts::all_pages_render` | — |
| REG-OTHER-004 | **更新 Celery 任务队列配置** | 邮件发送、搜索索引重建、维护任务 | 异步邮件不发送、搜索索引不更新 | `test_regression.py::test_regression_celery_config` | — |
| REG-OTHER-005 | **修改 Elasticsearch 索引映射** | 搜索功能 | 搜索过滤/排序字段名不匹配 | `test_regression.py::test_regression_es_mapping_change` | — |
| REG-OTHER-006 | **修改缓存键格式** | 全站缓存化接口 | 缓存未命中→性能下降；或缓存键冲突→返回错误数据 | `test_regression.py::test_regression_cache_key_change` | — |
| REG-OTHER-007 | **升级第三方依赖（Stripe SDK、FastAPI等）** | 支付接口、API 路由 | Stripe 接口变更导致支付失败 | `e2e/regression-payment.spec.ts::payment_flow` | — |
| REG-OTHER-008 | **前端 SSR 组件树变更** | SEO、首屏加载 | 页面 SSR 内容不完整、搜索引擎收录异常 | `e2e/regression-ssr.spec.ts::ssr_content_check` | — |

---

## 七、安全测试维度

| 测试编号 | 测试功能 | 测试步骤 | 对应测试脚本 | 预期结果 | 实际结果 |
|---------|---------|---------|-------------|---------|---------|
| TC-SEC-001 | **JWT 伪造/篡改** | 1. 修改JWT payload 后重签名 2. 访问受保护接口 | `test_security.py::test_jwt_tampering` | 返回401 | — |
| TC-SEC-002 | **Token 重放攻击** | 1. 捕获合法 token 2. 延迟重放（超过有效期） | `test_security.py::test_token_replay` | 返回401 | — |
| TC-SEC-003 | **越权访问其他用户数据** | 1. 用户A token 访问用户B的订单详情 | `test_security.py::test_unauthorized_access_others_data` | 返回404 | ✅ |
| TC-SEC-004 | **普通用户越权访问管理接口** | 1. 普通用户 GET `/admin/stats` | `test_security.py::test_regular_user_admin_access` | 返回403 | ✅ |
| TC-SEC-005 | **IDOR（参数篡改）** | 1. POST `/orders` 将 `user_id` 强制改为他人ID | `test_security.py::test_idor_parameter_tampering` | 后端使用JWT中的 session user_id，忽略请求体中的 user_id | — |
| TC-SEC-006 | **Rate Limiting** | 1. 快速连续发送100次登录请求 | `test_security.py::test_rate_limiting` | 达到阈值后返回429或暂时锁定 | — |
| TC-SEC-007 | **批量注册** | 1. 并发10次注册不同邮箱 | `test_security.py::test_bulk_registration` | 全部注册成功或触发风控（视策略） | — |
| TC-SEC-008 | **文件上传安全** | 1. POST `/admin/upload` 上传 `.exe` 文件 | `test_security.py::test_upload_malicious_file` | 文件类型校验拒绝 | — |
| TC-SEC-009 | **大文件上传拒绝** | 1. POST `/admin/upload` 上传 100MB 文件 | `test_security.py::test_upload_too_large` | 返回413或422 | — |
| TC-SEC-010 | **路径遍历** | 1. GET `/static/uploads/../../etc/passwd` | `test_security.py::test_path_traversal` | 返回404或禁止访问 | — |

---

## 八、分阶段实施计划

### Phase 1（P0）— 核心路径加固 ✅
> 目标：确保现有 466 项测试全部通过 + 新增安全/边界测试
> **实际结果：已全部完成**

| 任务 | 交付物 | 状态 |
|------|-------|------|
| 新增景点收藏 CRUD 测试 | `test_attraction_wishlist_crud.py` | ✅ |
| 新增景点收藏 API 测试 | 扩展 `test_business_flow_enhanced.py` | ✅ |
| 并发下单防超卖完善 | `test_concurrency.py`（13项） | ✅ |
| 安全测试（JWT/IDOR/上传） | `test_security.py`（17项） | ✅ |

### Phase 2（P1）— 逆向 + 边界扩展 ✅
> 目标：覆盖所有中断/异常流 + 弱网/极限值
> **实际结果：已全部完成，发现并修复 3 个后端 Bug**

| 任务 | 交付物 | 状态 |
|------|-------|------|
| Negative Path 自动化 | `test_negative_path.py`（14项）+ `negative-flow.spec.ts`（4项） | ✅ |
| Edge Case 场景 | `test_edge_cases.py`（10项） | ✅ |
| 多语言逆向 E2E | `negative-flow.spec.ts` | ✅ |

### Phase 3（P1）— 回归测试体系 ✅
> 目标：构建可重复执行的回归套件，每次部署前运行
> **实际结果：已全部完成，CI 已集成回归安全门禁**

| 任务 | 交付物 | 状态 |
|------|-------|------|
| 跨模块回归分析自动化 | `test_regression.py`（12项） | ✅ |
| 回归 E2E spec 文件 | `regression-*.spec.ts`（6个文件） | ✅ |
| 回归 CI 集成 | `.github/workflows/ci.yml` 回归安全门禁 + E2E 回归 job | ✅ |

---

## 九、测试脚本文件索引

| 文件路径 | 新增用例数 | 类型 |
|---------|-----------|------|
| `src/backend/tests/test_api/test_business_flow_enhanced.py` | +5 | API 集成测试 |
| `src/backend/tests/test_crud/test_attraction_wishlist_crud.py` | +8 | CRUD 单元测试 |
| `src/backend/tests/test_concurrency.py` | +13 | 并发/竞态测试 |
| `src/backend/tests/test_negative_path.py` | **14** ✅ | 逆向操作测试 |
| `src/backend/tests/test_edge_cases.py` | **10** ✅ | 边界/极限测试 |
| `src/backend/tests/test_regression.py` | **12** ✅ | 回归影响测试 |
| `src/backend/tests/test_security.py` | +17 | 安全渗透测试 |
| `tests/e2e/negative-flow.spec.ts` | **4** ✅ | E2E 逆向操作 |
| `tests/e2e/regression-wishlist.spec.ts` | **4** ✅ | E2E 收藏回归 |
| `tests/e2e/regression-order.spec.ts` | **4** ✅ | E2E 订单回归 |
| `tests/e2e/regression-ssr.spec.ts` | **3** ✅ | SSR 回归 |
| `tests/e2e/regression-i18n.spec.ts` | **3** ✅ | 国际化回归 |
| `tests/e2e/regression-auth.spec.ts` | **3** ✅ | 认证回归 |
| `tests/e2e/regression-payment.spec.ts` | **3** ✅ | 支付回归 |

---

## 十、执行标准

### 通过标准 ✅
- 所有 P0 测试 100% 通过 ✅
- P1 测试 ≥95% 通过 ✅
- 无 `test.skip` 忽略关键路径 ✅
- 所有通过标准记录在 `test-results/` 目录 ✅

### 质量门禁
1. ✅ 新增功能必须对应新增测试（覆盖率不下降）
2. ✅ **每次 PR 合入前运行 CI 回归安全门禁**（`.github/workflows/ci.yml`）
3. ✅ 回归测试不通过 → 阻断合并
4. ⏳ 每两周运行一次全量安全测试（建议配置 cron schedule）

### 当前测试总览

```bash
后端全量: 429 passed  |  7 skipped  |  0 failed  |  0 xfailed
E2E 测试:  19 个 spec 文件（13 核心 + 6 回归）
新增测试:  36 项（Phase 2 + Phase 3）
Bug 修复:  3 项（重复支付、contact_name/email 超长导致 500）
CI 门禁:  全量测试 → 回归安全门禁 → E2E 回归
```

---

> **文档负责人**: QA Team  
> **上次更新**: 2026-06-05  
> **版本**: v2.8（Phase 2 + Phase 3 全部完成）  
> **关联文档**: [测试用例-全业务流程.md](./测试用例-全业务流程.md)  
